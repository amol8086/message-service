import Ember from 'ember';

export default Ember.Route.extend({
		/*model: function() {
        // this can be the another aproach where we define a new record here and use the same in the hbs and controller to save.
        return this.store.createRecord('message');
    }*/
});
